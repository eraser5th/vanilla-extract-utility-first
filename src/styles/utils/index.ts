import { layoutProperties, layoutShorthands } from "./layout";

export * from "./layout";

export const properties = {
  ...layoutProperties,
};

export const shorthands = {
  ...layoutShorthands,
};
